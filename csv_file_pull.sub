import csv

with open('car.csv') as csvfile:
  readcsv = csv.reader(csvfile, delimiter = ',')
  dates = []
  colours = []
  for row in readcsv:
    colour = row[3]
    date = row[0]
  
    dates.append(date)
    colours.append(colour)
   print (dates)
   print (colours)
